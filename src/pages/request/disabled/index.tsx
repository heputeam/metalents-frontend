import React from 'react';
import RequestDisable from '../components/RequestDisable';

const RequestDisabledPage: React.FC = () => {
  return <RequestDisable />;
};

export default RequestDisabledPage;
